# SSE (Server-Sent Events) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O aluno vê a barra de progresso da jornada avançar em tempo real no dashboard, sem recarregar a página, quando um evento Kafka é processado pelo odyssey.

**Architecture:** Quatro camadas em sequência: (1) `@enxoval/http` ganha a função `sseRoute` que configura headers SSE e expõe `send`/`signal` ao handler; (2) `odyssey` ganha um `journeyEventBus` (EventEmitter singleton) e uma rota SSE que escuta o bus por `journeyId`; (3) cada controller Kafka do odyssey emite no bus após atualizar o step; (4) `imperium` cria uma rota proxy que valida o JWT via query param e faz pipe do stream do odyssey; (5) `arrakis` usa um hook `useJourneyStream` que abre um `EventSource` e atualiza o estado local da jornada.

**Tech Stack:** Node.js `EventEmitter`, Fastify `reply.raw`, browser `EventSource` API, `@enxoval/http` (bump 1.0.21 → 1.0.22), `@enxoval/types` `asyncFn`.

---

## Mapa de arquivos

| Arquivo | Ação |
|---|---|
| `enxoval/http/server/index.ts` | Modificar — adicionar `sseRoute` |
| `enxoval/http/index.ts` | Modificar — exportar `sseRoute` |
| `enxoval/http/package.json` | Modificar — bump para 1.0.22 |
| `odyssey/src/wire/journey-event-bus.ts` | Criar — EventEmitter singleton |
| `odyssey/src/diplomat/http-server/journey-stream.ts` | Criar — rota SSE |
| `odyssey/src/diplomat/http-server/index.ts` | Modificar — registrar rota stream |
| `odyssey/src/app.ts` | Modificar — excluir rota stream do auth |
| `odyssey/src/controllers/diagnostic-triggered.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/diagnostic-completed.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/analysis-started.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/analysis-finished.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/curriculum-generated.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/content-dispatched.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/student-engagement-received.ts` | Modificar — emit após updateStep |
| `odyssey/src/controllers/progress-milestone-reached.ts` | Modificar — emit após updateStep |
| `odyssey/package.json` | Modificar — bump @enxoval/http para ^1.0.22 |
| `imperium/src/diplomat/http-server/journey-stream.ts` | Criar — proxy SSE com auth |
| `imperium/src/app.ts` | Modificar — registrar rota stream + excluir do auth |
| `imperium/package.json` | Modificar — bump @enxoval/http para ^1.0.22 |
| `arrakis/src/hooks/useJourneyStream.ts` | Criar — hook EventSource |
| `arrakis/src/pages/Dashboard.tsx` | Modificar — usar hook em JourneyCard ativo |

---

## Task 1: Adicionar `sseRoute` ao @enxoval/http

**Files:**
- Modify: `enxoval/http/server/index.ts`
- Modify: `enxoval/http/index.ts`
- Modify: `enxoval/http/package.json`

- [ ] **Step 1: Adicionar `sseRoute` em `server/index.ts`**

Adicionar a função ao final do arquivo, antes de qualquer export (após as funções `put`, `patch`, `del` existentes):

```ts
export function sseRoute<TParams, TQuery = Record<string, string>>(
  path: string,
  handler: (
    params: TParams,
    query: TQuery,
    send: (data: object) => void,
    signal: AbortSignal,
  ) => Promise<void>,
): void {
  app.get<{ Params: TParams; Querystring: TQuery }>(path, async (request, reply) => {
    reply.hijack();
    reply.raw.setHeader('Content-Type', 'text/event-stream');
    reply.raw.setHeader('Cache-Control', 'no-cache');
    reply.raw.setHeader('Connection', 'keep-alive');
    reply.raw.setHeader('Access-Control-Allow-Origin', '*');
    reply.raw.flushHeaders();

    const controller = new AbortController();
    const onClose = () => controller.abort();
    request.raw.on('close', onClose);

    const send = (data: object) => {
      if (!reply.raw.destroyed) {
        reply.raw.write(`data: ${JSON.stringify(data)}\n\n`);
      }
    };

    try {
      await handler(request.params as TParams, request.query as TQuery, send, controller.signal);
    } finally {
      request.raw.off('close', onClose);
      if (!reply.raw.destroyed) reply.raw.end();
    }
  });
}
```

- [ ] **Step 2: Exportar `sseRoute` em `index.ts`**

Adicionar `sseRoute` na linha de export existente:

```ts
export { get, getWith, getWithAuth, post, postOk, put, patch, del, html, listen, close, inject, addPreHandler, sseRoute } from './server/index';
```

- [ ] **Step 3: Bump de versão**

Em `enxoval/http/package.json`, alterar `"version"` de `"1.0.21"` para `"1.0.22"`.

- [ ] **Step 4: Build e verificação de tipos**

```bash
cd enxoval/http && npx tsc --noEmit
```
Esperado: sem erros.

- [ ] **Step 5: Publicar nova versão**

```bash
git add enxoval/http/server/index.ts enxoval/http/index.ts enxoval/http/package.json
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(http): add sseRoute for Server-Sent Events"
git tag v1.0.26
git push origin main && git push origin v1.0.26
```
Aguardar o GitHub Actions publicar e abrir PRs de bump nos serviços (ou fazer o bump manualmente nos próximos passos).

---

## Task 2: Criar journeyEventBus no odyssey

**Files:**
- Create: `odyssey/src/wire/journey-event-bus.ts`

- [ ] **Step 1: Criar o EventEmitter singleton**

```ts
import { EventEmitter } from 'node:events';

const bus = new EventEmitter();
bus.setMaxListeners(0);

export type JourneyUpdate = { id: string; currentStep: string; status: string };

export function emit(journeyId: string, update: JourneyUpdate): void {
  bus.emit(journeyId, update);
}

export function on(journeyId: string, cb: (update: JourneyUpdate) => void): void {
  bus.on(journeyId, cb);
}

export function off(journeyId: string, cb: (update: JourneyUpdate) => void): void {
  bus.off(journeyId, cb);
}
```

- [ ] **Step 2: Verificar tipos**

```bash
cd odyssey && npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add odyssey/src/wire/journey-event-bus.ts
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(odyssey): add journeyEventBus singleton"
```

---

## Task 3: Criar rota SSE no odyssey

**Files:**
- Create: `odyssey/src/diplomat/http-server/journey-stream.ts`
- Modify: `odyssey/src/diplomat/http-server/index.ts`
- Modify: `odyssey/src/app.ts`
- Modify: `odyssey/package.json`

- [ ] **Step 1: Bump @enxoval/http no package.json do odyssey**

Em `odyssey/package.json`, alterar `"@enxoval/http": "^1.0.21"` para `"@enxoval/http": "^1.0.22"`. Rodar:

```bash
cd odyssey && npm install
```

- [ ] **Step 2: Criar `journey-stream.ts`**

```ts
import { sseRoute } from '@enxoval/http';
import * as journeyEventBus from '../../wire/journey-event-bus';

export function registerJourneyStreamRoute(): void {
  sseRoute<{ journeyId: string }, Record<string, never>>(
    '/journeys/:journeyId/stream',
    async ({ journeyId }, _query, send, signal) => {
      const listener = (update: journeyEventBus.JourneyUpdate) => send(update);
      journeyEventBus.on(journeyId, listener);
      await new Promise<void>((resolve) => {
        signal.addEventListener('abort', () => {
          journeyEventBus.off(journeyId, listener);
          resolve();
        });
      });
    },
  );
}
```

- [ ] **Step 3: Registrar rota em `index.ts`**

Em `odyssey/src/diplomat/http-server/index.ts`, adicionar o import e a chamada:

```ts
import { registerJourneyRoutes } from './journey';
import { registerJourneysListRoutes } from './journeys-list';
import { registerRepublishRoutes } from './republish';
import { registerJourneyStreamRoute } from './journey-stream';

export function setupRoutes(): void {
  registerJourneyRoutes();
  registerJourneysListRoutes();
  registerRepublishRoutes();
  registerJourneyStreamRoute();
}
```

- [ ] **Step 4: Excluir rota stream do auth em `app.ts`**

Em `odyssey/src/app.ts`, adicionar o pattern à lista de exclusão:

```ts
export function buildApp(): void {
  setupAuth({ exclude: ['/health', '/docs', '/journeys/:journeyId/stream'] });
  // ... restante igual
}
```

- [ ] **Step 5: Verificar tipos**

```bash
cd odyssey && npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add odyssey/src/diplomat/http-server/journey-stream.ts odyssey/src/diplomat/http-server/index.ts odyssey/src/app.ts odyssey/package.json odyssey/package-lock.json
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(odyssey): add SSE stream route for journey updates"
```

---

## Task 4: Emitir no bus em cada controller do odyssey

**Files:**
- Modify: `odyssey/src/controllers/diagnostic-triggered.ts`
- Modify: `odyssey/src/controllers/diagnostic-completed.ts`
- Modify: `odyssey/src/controllers/analysis-started.ts`
- Modify: `odyssey/src/controllers/analysis-finished.ts`
- Modify: `odyssey/src/controllers/curriculum-generated.ts`
- Modify: `odyssey/src/controllers/content-dispatched.ts`
- Modify: `odyssey/src/controllers/student-engagement-received.ts`
- Modify: `odyssey/src/controllers/progress-milestone-reached.ts`

Em cada controller, após `journeyDb.updateStep(...)`, adicionar o import e a chamada de emit.

O padrão para cada controller é:

```ts
// Adicionar import no topo:
import * as journeyEventBus from '../wire/journey-event-bus';

// Após journeyDb.updateStep(...):
journeyEventBus.emit(previous.journeyId, {
  id: previous.journeyId,
  currentStep: '<NOME_DO_STEP_DESTINO>',
  status: 'active',
});
```

- [ ] **Step 1: `diagnostic-triggered.ts`** — emit `{ id: previous.journeyId, currentStep: 'DIAGNOSTIC_COMPLETED', status: 'active' }`

- [ ] **Step 2: `diagnostic-completed.ts`** — emit `{ id: previous.journeyId, currentStep: 'ANALYSIS_STARTED', status: 'active' }`

- [ ] **Step 3: `analysis-started.ts`** — emit `{ id: previous.journeyId, currentStep: 'ANALYSIS_FINISHED', status: 'active' }`

- [ ] **Step 4: `analysis-finished.ts`** — emit `{ id: previous.journeyId, currentStep: 'CURRICULUM_GENERATED', status: 'active' }`

- [ ] **Step 5: `curriculum-generated.ts`** — emit `{ id: previous.journeyId, currentStep: 'CONTENT_DISPATCHED', status: 'active' }`

- [ ] **Step 6: `content-dispatched.ts`** — emit `{ id: previous.journeyId, currentStep: 'STUDENT_ENGAGEMENT_RECEIVED', status: 'active' }`

- [ ] **Step 7: `student-engagement-received.ts`** — emit `{ id: previous.journeyId, currentStep: 'PROGRESS_MILESTONE_REACHED', status: 'active' }`

- [ ] **Step 8: `progress-milestone-reached.ts`** — emit `{ id: previous.journeyId, currentStep: 'JOURNEY_COMPLETED', status: 'completed' }`

> **Nota:** O `journey-initiated.ts` não precisa emitir — não existe passo anterior para emitir a partir dele. O `republish.ts` também não emite.

- [ ] **Step 9: Verificar tipos**

```bash
cd odyssey && npx tsc --noEmit
```
Esperado: sem erros.

- [ ] **Step 10: Rodar testes**

```bash
cd odyssey && npm test
```
Esperado: todos os testes passando.

- [ ] **Step 11: Commit**

```bash
git add odyssey/src/controllers/
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(odyssey): emit journey updates on journeyEventBus after each step"
```

---

## Task 5: Criar rota proxy SSE no imperium

**Files:**
- Create: `imperium/src/diplomat/http-server/journey-stream.ts`
- Modify: `imperium/src/app.ts`
- Modify: `imperium/package.json`

- [ ] **Step 1: Bump @enxoval/http no package.json do imperium**

Em `imperium/package.json`, alterar `"@enxoval/http": "^1.0.21"` para `"@enxoval/http": "^1.0.22"`. Rodar:

```bash
cd imperium && npm install
```

- [ ] **Step 2: Criar `journey-stream.ts`**

```ts
import { sseRoute } from '@enxoval/http';
import { UnauthorizedError } from '@enxoval/types';
import * as jwt from 'jsonwebtoken';

export function registerJourneyStreamRoute(): void {
  sseRoute<{ journeyId: string }, { token: string }>(
    '/journeys/:journeyId/stream',
    async ({ journeyId }, { token }, send, signal) => {
      if (!token) throw new UnauthorizedError('Token required');

      try {
        jwt.verify(token, process.env.JWT_SECRET as string);
      } catch {
        throw new UnauthorizedError('Invalid token');
      }

      const upstream = await fetch(
        `${process.env.ODYSSEY_URL}/journeys/${journeyId}/stream`,
        { signal },
      );

      if (!upstream.ok || !upstream.body) return;

      const reader = upstream.body.getReader();
      const decoder = new TextDecoder();
      let buffer = '';

      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          buffer += decoder.decode(value, { stream: true });
          const chunks = buffer.split('\n\n');
          buffer = chunks.pop() ?? '';
          for (const chunk of chunks) {
            const line = chunk.split('\n').find((l) => l.startsWith('data: '));
            if (line) {
              try {
                send(JSON.parse(line.slice(6)) as object);
              } catch {
                // ignore malformed data
              }
            }
          }
        }
      } finally {
        reader.cancel();
      }
    },
  );
}
```

> **Nota:** Se `jsonwebtoken` não for dependência direta do `imperium`, adicionar: `npm install jsonwebtoken @types/jsonwebtoken --save` no diretório `imperium/`.

- [ ] **Step 3: Registrar rota e excluir do auth em `app.ts`**

```ts
import { get } from '@enxoval/http';
import { setupAuth } from '@enxoval/auth';
import { registerAuthRoutes } from './diplomat/http-server/auth';
import { registerRegisterRoutes } from './diplomat/http-server/register';
import { registerMeRoutes } from './diplomat/http-server/me';
import { registerStudentRoutes } from './diplomat/http-server/students';
import { registerJourneyRoutes } from './diplomat/http-server/journeys';
import { registerJourneyStreamRoute } from './diplomat/http-server/journey-stream';

export function buildApp(): void {
  get('/health', async () => ({ status: 'ok' }));
  setupAuth({ exclude: ['/health', '/auth/login', '/users/register', '/journeys/:journeyId/stream'] });
  registerAuthRoutes();
  registerRegisterRoutes();
  registerMeRoutes();
  registerStudentRoutes();
  registerJourneyRoutes();
  registerJourneyStreamRoute();
}
```

- [ ] **Step 4: Verificar tipos**

```bash
cd imperium && npx tsc --noEmit
```

- [ ] **Step 5: Rodar testes**

```bash
cd imperium && npm test
```

- [ ] **Step 6: Commit**

```bash
git add imperium/src/diplomat/http-server/journey-stream.ts imperium/src/app.ts imperium/package.json imperium/package-lock.json
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(imperium): add SSE proxy route for journey stream"
```

---

## Task 6: Hook `useJourneyStream` e integração no Dashboard

**Files:**
- Create: `arrakis/src/hooks/useJourneyStream.ts`
- Modify: `arrakis/src/pages/Dashboard.tsx`

- [ ] **Step 1: Criar o hook**

```ts
import { useEffect } from 'react';
import type { JourneyData } from '../api/imperium';

const BASE = import.meta.env.VITE_IMPERIUM_URL as string;

type Update = { id: string; currentStep: string; status: string };

export function useJourneyStream(
  journeyId: string,
  token: string,
  onUpdate: (update: Update) => void,
): void {
  useEffect(() => {
    const url = `${BASE}/journeys/${journeyId}/stream?token=${encodeURIComponent(token)}`;
    const es = new EventSource(url);

    es.onmessage = (e: MessageEvent<string>) => {
      try {
        onUpdate(JSON.parse(e.data) as Update);
      } catch {
        // ignore malformed events
      }
    };

    return () => {
      es.close();
    };
  }, [journeyId, token, onUpdate]);
}
```

- [ ] **Step 2: Usar o hook no JourneyCard do Dashboard**

No `Dashboard.tsx`, extrair `JourneyCard` para usar o hook. Localizar a função `JourneyCard` e adicionar o hook e a atualização de estado:

```tsx
function JourneyCard({ journey, token, onUpdate }: {
  journey: JourneyData;
  token: string;
  onUpdate: (id: string, update: { currentStep: string; status: string }) => void;
}) {
  const [open, setOpen] = useState(false);

  useJourneyStream(
    journey.id,
    token,
    useCallback(
      (update) => onUpdate(update.id, { currentStep: update.currentStep, status: update.status }),
      [onUpdate],
    ),
  );

  // ... restante do JSX igual ao original
}
```

No componente `Dashboard`, adicionar a função `handleJourneyUpdate` e passar para `JourneyCard`:

```tsx
function handleJourneyUpdate(id: string, update: { currentStep: string; status: string }) {
  setJourneys((prev) =>
    prev.map((j) => (j.id === id ? { ...j, ...update } : j)),
  );
}

// No render, passar token e onUpdate:
{myJourneys.map(j => (
  <JourneyCard key={j.id} journey={j} token={token!} onUpdate={handleJourneyUpdate} />
))}
```

Adicionar o import do hook no topo do Dashboard:

```tsx
import { useJourneyStream } from '../hooks/useJourneyStream';
```

- [ ] **Step 3: Verificar tipos**

```bash
cd arrakis && npx tsc --noEmit
```

- [ ] **Step 4: Build**

```bash
cd arrakis && npm run build
```

- [ ] **Step 5: Commit**

```bash
git add arrakis/src/hooks/useJourneyStream.ts arrakis/src/pages/Dashboard.tsx
git -c user.name="Joao Vitor" -c user.email="joaorabello99@gmail.com" commit -m "feat(arrakis): add useJourneyStream hook and wire into Dashboard"
```

---

## Verificação manual

1. Subir a plataforma completa: `docker compose up --build`
2. Abrir o dashboard com uma jornada ativa
3. Publicar um evento Kafka manualmente via kafka-ui (`localhost:8081`) ou aguardar o ciclo automático
4. Observar: a barra de progresso da jornada avança sem recarregar a página
5. Inspecionar DevTools → Network → `stream` — deve aparecer uma conexão SSE aberta com eventos chegando
6. Fechar a aba e verificar nos logs do odyssey que o listener foi removido (sem memory leak)
